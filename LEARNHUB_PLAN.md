# LearnHub 开发与学习计划

> 项目定位：LearnHub 是一个用于学习 Spring Boot、Spring AI 和 RAG 的可运行基础项目。它强调理解、实验和后续改进，不宣称已经达到生产级高可用或任何收益保证。
>
> 开始日期：2026-07-18  
> 后端目录：`D:\LearnHub\LearnHubBackend`

## 1. 学习目标

完成本项目后，应当能够独立完成以下工作：

- 使用 Java 21 和 Spring Boot 开发结构清晰的 REST API。
- 解释 IOC、AOP、MVC、自动配置、事务和 Spring Security 的工作方式。
- 独立设计 MySQL 表、索引和事务，并能分析常见慢 SQL。
- 使用 Redis 解决缓存、限流、幂等、原子扣减等问题。
- 使用 RabbitMQ 完成异步处理、重试、死信和幂等消费。
- 使用 MinIO 管理文件，完成文档上传、解析和生命周期管理。
- 实现 RAG 知识库问答、向量检索、流式输出和引用溯源。
- 编写单元测试、集成测试并进行基础并发压测。
- 使用 Docker Compose 部署完整系统，并具备基础监控和排错能力。
- 在面试中说明每项技术解决的问题、设计取舍和故障处理方式。

“掌握”不等于项目中出现过某个依赖，而是能够回答：

1. 为什么使用它？
2. 不使用它会发生什么？
3. 它引入了什么新问题？
4. 出现故障时如何定位？

## 2. 产品定义

LearnHub 是一个 AI 驱动的个人知识库与学习平台。用户可以上传 PDF、Word、Markdown 等资料，系统异步解析文档并构建向量知识库。用户能够基于自己的资料进行 AI 问答、查看引用来源、生成练习题、管理错题和复习计划。

### 2.1 核心用户流程

1. 用户注册、登录并创建知识库。
2. 用户上传学习资料，文件存入 MinIO。
3. 后端创建文档记录并向 RabbitMQ 发送解析任务。
4. Worker 解析文档、切分文本、生成向量并写入向量数据库。
5. 用户向知识库提问，后端检索相关文本并调用大模型。
6. 回答通过 SSE 流式返回，同时展示引用文档和片段。
7. 用户让 AI 根据资料生成测试题，并将错题加入复习计划。
8. 每次 AI 调用扣除额度；用户可以通过模拟订单获得额度。
9. 管理员查看用户、AI 调用量、失败任务和系统指标。

### 2.2 系统结构

```text
Vue 3
   |
Nginx
   |
Spring Boot API
   |-- MySQL：业务数据
   |-- Redis：缓存、限流、幂等、额度
   |-- RabbitMQ：文档解析任务
   |-- MinIO：原始文件
   |-- Qdrant：文本向量
   |-- 大模型 API：对话、Embedding、出题
   `-- Prometheus/Grafana：指标与监控
```

第一阶段采用“模块化单体”。只有单体版本稳定并完成测试后，才考虑将文档处理 Worker 或 AI 模块拆成独立服务。

## 3. 技术栈

### 3.1 后端

- Java 21
- Spring Boot 3.5.16
- Spring MVC
- Spring Validation
- Spring Security + JWT
- MyBatis 或 MyBatis-Plus（重要 SQL 尽量手写）
- MySQL 8
- Flyway
- Redis + Redisson
- RabbitMQ
- MinIO
- Spring AI
- Qdrant
- JUnit 5、Mockito、Testcontainers
- Spring Boot Actuator、Micrometer、Prometheus、Grafana
- Maven、Docker Compose、Nginx

学习资料应优先使用 Spring Boot 3 的现代教程。需要重点避开 `javax.*`、`WebSecurityConfigurerAdapter` 等旧式写法；Spring Boot 4 仅作为项目完成后的可选迁移练习。

### 3.2 前端

- Vue 3
- TypeScript
- Vite
- Element Plus
- Axios

前端以完成业务闭环为目标，预计不超过总时间的 15%～20%。

## 4. 当前项目状态

### 4.1 已完成

- [x] 创建 Maven 父项目。
- [x] 创建 `learnhub-application`。
- [x] 创建 `learnhub-common`。
- [x] 创建 `learnhub-user`。
- [x] 创建 `learnhub-knowledge`。
- [x] 创建 `learnhub-ai`。
- [x] 创建 `learnhub-study`。
- [x] 创建 `learnhub-credit`。
- [x] 创建 `learnhub-infrastructure`。
- [x] Part 1（工程骨架与 Web 基础）完成：应用启动与健康检查、统一响应/异常、示例接口、自动化测试、OpenAPI、Docker Compose。

### 4.2 启动开发前需要确认

- [x] 统一所有模块的 Java 版本为 21。
- [x] 删除子模块重复的编译版本配置，由父 POM 统一管理。
- [x] 确认本机 Maven 使用的确实是 JDK 21。
- [x] 将项目统一为 Spring Boot 3.5.16，并验证第三方依赖能够构建。
- [x] 为后端仓库补充 `.gitignore` 和 `README.md`；Git 策略见决策记录（项目根目录刻意不做仓库，后续拆为三个仓库）。
- [x] 明确模块依赖方向，避免所有模块互相依赖。

> 2026-07-18 已完成 POM 整理：所有模块统一使用 Java 21 和 Spring Boot 3.5.16，第三方版本由父 POM 集中管理，并通过完整 Maven Reactor 构建。

## 5. 模块职责

### `learnhub-application`

- Spring Boot 启动类。
- 组合各业务模块。
- 全局配置和环境配置文件。
- 不堆放具体业务逻辑。

### `learnhub-common`

- 通用返回结构。
- 错误码和业务异常。
- 基础工具类。
- 通用常量。

只放真正跨模块、稳定的内容，避免变成无边界的“大杂烩”。

### `learnhub-user`

- 用户注册、登录和退出。
- Spring Security 与 JWT。
- 用户、角色、权限和审计日志。
- 登录失败限制和账号状态管理。

### `learnhub-knowledge`

- 知识库管理。
- 文档元数据与状态机。
- 文件上传、删除和访问权限。
- 文档解析任务的发起与结果保存。

### `learnhub-ai`

- 文档切分和 Embedding。
- Qdrant 向量写入与检索。
- RAG 问答。
- SSE 流式输出。
- 模型 Provider 抽象、超时、重试和调用记录。

### `learnhub-study`

- AI 生成题目。
- 答题、判分和错题本。
- 学习计划与复习提醒。
- 学习数据统计。

### `learnhub-credit`

- AI 使用额度。
- 额度流水。
- 模拟订单和支付回调。
- 并发扣减、回调幂等和超时关闭。

### `learnhub-infrastructure`

- MySQL、Redis、RabbitMQ、MinIO、Qdrant 等外部系统适配。
- 外部服务的配置、客户端和技术实现。
- 不承载具体业务规则。

## 6. 实施阶段路线（Part 1 ~ Part 8）

> 不按“周”排期，按阶段推进：完成一个 Part 并验收后再进入下一个。`docs/week-01/` 是 Part 1 的历史课程资料；后续 Part 的文档统一放在 `docs/part-XX/`。

## Part 1：工程骨架与 Web 基础（已完成）

### 学习内容

- 面向对象、接口和抽象类。
- 集合、泛型、异常。
- Lambda、Stream、Optional。
- IO/NIO、日期时间 API。
- 注解和反射基础。
- 线程、线程池、锁、`CompletableFuture`。
- Maven 生命周期与多模块依赖。

### 项目任务

- [x] 统一 JDK 和 Maven 配置。
- [x] 建立规范的包结构和模块依赖。
- [x] 创建启动类并成功启动应用。
- [x] 实现统一响应对象、错误码和全局异常处理。
- [x] 接入 Validation 和 OpenAPI。
- [x] 使用 Docker Compose 启动 MySQL、Redis、RabbitMQ、MinIO、Qdrant。
- [x] 创建 `dev` 环境配置，敏感信息使用环境变量。

### 验收标准

- [x] 父项目可以执行完整 Maven 构建。
- [x] 应用可启动并通过健康检查。
- [x] 一个示例接口包含成功、参数错误和业务错误测试。
- [x] 能画出模块依赖关系并解释每个模块职责。

> 回顾教学：测试与 OpenAPI 的讲解见 `docs/part-01/review-testing-and-openapi.md`；Part 2 的详细安排见 `docs/part-02/README.md`。

## Part 2：用户、登录与权限

### 学习内容

- Spring IOC、Bean 生命周期和依赖注入。
- Spring MVC 请求处理流程。
- Spring Security 过滤器链。
- JWT、密码哈希、RBAC。
- MySQL 表设计、索引和事务基础。

### 项目任务

- [ ] 使用 Flyway 创建用户、角色和关联表。
- [ ] 完成注册、登录、刷新令牌和退出。
- [ ] 使用 BCrypt 或 Argon2 保存密码。
- [ ] 配置 Spring Security 6/7 风格的 `SecurityFilterChain`。
- [ ] 实现普通用户和管理员权限。
- [ ] 增加登录失败限制和操作审计。

### 验收标准

- [ ] 密码不以明文存储。
- [ ] 未登录用户无法访问受保护接口。
- [ ] 普通用户无法调用管理员接口。
- [ ] Token 过期、伪造和退出后的行为均有测试。

## Part 3：知识库与文件管理

### 学习内容

- MySQL 事务、锁、索引和分页。
- 文件上传、哈希和对象存储。
- 领域状态与状态机。
- 用户数据隔离。

### 项目任务

- [ ] 创建知识库、文档和文档任务表。
- [ ] 完成知识库增删改查。
- [ ] 使用 MinIO 上传、下载和删除文件。
- [ ] 校验文件大小、类型和所有权。
- [ ] 通过 SHA-256 检测重复文件。
- [ ] 实现文档状态：`UPLOADED -> PARSING -> EMBEDDING -> COMPLETED/FAILED`。

### 验收标准

- [ ] 用户无法读取或修改他人的知识库。
- [ ] 文件上传失败时不会产生错误的完整状态。
- [ ] 删除操作明确处理数据库、MinIO 和向量数据的一致性。
- [ ] 对列表和查询接口检查执行计划与索引使用情况。

## Part 4：RabbitMQ 异步文档处理

### 学习内容

- Exchange、Queue、Routing Key。
- ACK、重试、死信队列。
- 至少一次投递和重复消息。
- 幂等、最终一致性和补偿任务。

### 项目任务

- [ ] 上传成功后发送文档解析消息。
- [ ] 实现异步文档解析消费者。
- [ ] 实现消息确认、有限重试和死信队列。
- [ ] 使用任务 ID 或业务唯一键保证幂等消费。
- [ ] 支持查看进度和重新执行失败任务。
- [ ] 记录每次失败原因和重试次数。

### 验收标准

- [ ] 同一消息重复发送不会重复生成业务数据。
- [ ] 消费过程中应用退出后，任务能够恢复。
- [ ] 永久失败的消息进入死信队列，而不是无限重试。
- [ ] RabbitMQ 暂时不可用时有明确的错误和恢复策略。

## Part 5：RAG 与流式问答

### 学习内容

- 文档切分、Embedding 和向量相似度。
- RAG 基本流程。
- Prompt 结构与上下文限制。
- SSE、响应式流和客户端断开处理。
- 第三方 API 的超时、重试、降级和成本控制。

### 项目任务

- [ ] 解析 PDF、Word 和 Markdown 文本。
- [ ] 设计带重叠窗口的文本切分策略。
- [ ] 调用 Embedding 模型并写入 Qdrant。
- [ ] 根据问题检索相关片段。
- [ ] 组装 Prompt 并调用聊天模型。
- [ ] 使用 SSE 流式返回答案。
- [ ] 返回引用文档、页码或片段定位信息。
- [ ] 保存会话、消息和模型调用记录。

### 验收标准

- [ ] 回答能够显示可核对的引用来源。
- [ ] 模型超时、限流、返回错误时不会一直占用连接。
- [ ] 用户中断连接时能够停止或清理后端任务。
- [ ] 能解释切分大小、Top-K 等参数的选择依据。

## Part 6：Redis、额度与订单并发

### 学习内容

- Cache Aside 模式和缓存一致性。
- Redis 数据结构、过期策略和 Lua。
- 本地锁、乐观锁和分布式锁的适用范围。
- 接口幂等、订单状态机和 `BigDecimal`。

### 项目任务

- [ ] 缓存热点知识库信息。
- [ ] 实现用户/IP 维度的接口限流。
- [ ] 实现 AI 额度查询、赠送和流水记录。
- [ ] 使用原子操作或 Lua 完成并发额度扣减。
- [ ] 创建模拟额度订单。
- [ ] 实现支付回调幂等。
- [ ] 定时关闭超时订单。

### 验收标准

- [ ] 用户剩余一次额度时，并发十次最多成功一次。
- [ ] 同一支付回调执行多次只入账一次。
- [ ] 额度流水能够还原每次余额变化。
- [ ] 能说明缓存删除失败、数据库提交失败等情况如何处理。

## Part 7：学习功能、测试、性能与部署

### 学习内容

- JUnit 5、Mockito、Spring 集成测试。
- Testcontainers。
- JVM 内存、线程、GC 和常用诊断方法。
- SQL 慢查询与简单压测。
- Docker、Nginx 和可观测性。

### 项目任务

- [ ] 根据知识库生成选择题。
- [ ] 实现答题、判分、错题本和复习计划。
- [ ] 编写核心单元测试和集成测试。
- [ ] 对额度扣减、问答接口进行并发压测。
- [ ] 接入 Actuator、Prometheus 和 Grafana。
- [ ] 使用 Docker Compose 部署全套环境。
- [ ] 完成 README、架构图、ER 图和接口文档。

### 验收标准

- [ ] 核心业务测试可重复运行。
- [ ] 能根据日志中的 Trace ID 还原一次请求。
- [ ] 至少定位并优化一个慢 SQL 或慢接口。
- [ ] 新机器按照 README 可以启动项目。

## Part 8：项目收尾与有限优化

### 项目任务

- [x] 清理本轮已经确认的重复代码、无效配置和不必要依赖。
- [x] 完成一次缓存、安全、事务、资源和并发边界优化。
- [ ] 进行一次完整安全和权限自查。
- [ ] 整理压测结果、问题记录和设计决策。
- [ ] 录制 5～10 分钟项目演示。
- [ ] 准备 10～15 个项目面试问题。
- [ ] 如项目起始版本不是 Boot 4，建立单独分支进行升级并记录兼容问题。
- [ ] 仅在单体稳定后，考虑拆分文档 Worker 或 AI Service。

### 可选 Spring Cloud 内容

- Spring Cloud Gateway
- Nacos 注册与配置中心
- OpenFeign
- Sentinel
- 分布式链路追踪

微服务不是本项目完成的必要条件。没有部署、测试和可观测性的多个服务，只是增加了启动进程数量。

## 7. 每个功能的开发流程

每次只实现一个小而完整的功能，按以下顺序进行：

```text
明确需求
  -> 设计数据表和状态变化
  -> 设计 API
  -> 只学习当前需要的知识
  -> 自己实现
  -> 对高风险逻辑补测试；普通新功能先做接口和日志验收
  -> 主动制造异常和并发场景
  -> 排查并修复
  -> 更新文档
  -> 提交 Git
```

不要先看完一整套课程再开始项目。视频或文章用于解决当前问题，每次连续输入知识的时间尽量控制在 60～90 分钟以内。

### 每日建议

- 30 分钟：算法题。
- 60 分钟：Java、数据库、网络或操作系统基础。
- 3 小时：项目开发。
- 30 分钟：测试、压测或排错。
- 30 分钟：整理问题、结论和第二天任务。

## 8. Definition of Done

一个功能只有同时满足以下条件才可以标记完成：

- [ ] 正常流程可用。
- [ ] 参数边界经过校验。
- [ ] 身份与数据权限经过校验。
- [ ] 异常响应统一且不会泄漏敏感信息。
- [ ] 关键操作有结构化日志。
- [ ] 数据库变更使用 Flyway 管理。
- [ ] 高风险逻辑或回归 Bug 有相应的单元测试或集成测试；普通新功能不把测试作为硬门槛。
- [ ] 接口文档已更新。
- [ ] 已考虑重复请求、并发或服务暂时不可用的情况。
- [ ] 已完成一次有意义的 Git 提交。
- [ ] 能脱离代码解释设计与取舍。

## 9. 必须掌握的项目面试题

项目开发过程中逐步填写自己的答案：

1. 为什么先使用模块化单体，而不是直接使用微服务？
2. 一个登录请求经过 Spring Security 的哪些关键步骤？
3. JWT 退出登录和主动失效如何实现？
4. 如何保证用户只能访问自己的知识库？
5. 文档记录和 MinIO 文件如何处理一致性？
6. RabbitMQ 为什么可能产生重复消息？如何幂等？
7. 消费者执行到一半崩溃会发生什么？
8. 为什么文档解析需要异步，而不是直接在 HTTP 请求中执行？
9. RAG 的完整流程是什么？为什么仍可能产生幻觉？
10. SSE 与 WebSocket 有什么区别？这里为什么选 SSE？
11. 如何避免用户的一次额度被并发使用多次？
12. Redis 缓存与数据库不一致时如何处理？
13. 为什么支付回调必须幂等？
14. 项目中最慢的 SQL 是什么？如何发现并优化？
15. 如果流量增长十倍，最先出现瓶颈的组件可能是什么？
16. 如何定位一次响应很慢的 AI 问答请求？
17. JVM 出现内存持续增长时怎么排查？
18. Spring Boot 3 与 4 在本项目中遇到了哪些实际差异？

## 10. 暑假结束时的交付物

- [ ] 可访问的在线演示地址。
- [ ] 结构清晰的 Git 仓库。
- [ ] Docker Compose 一键启动环境。
- [ ] 完整 README 和启动说明。
- [ ] 系统架构图和数据库 ER 图。
- [ ] OpenAPI 接口文档。
- [ ] 单元测试和集成测试结果。
- [ ] 并发压测报告。
- [ ] 关键设计决策记录。
- [ ] Spring Boot 版本兼容/迁移记录。
- [ ] 项目演示视频。
- [ ] 个人项目复盘和面试问答文档。

## 11. Part 1 收尾清单（已完成）

按顺序完成，不提前接入 AI、MQ 或微服务：

- [x] 将 `learnhub-application` 的 Java 25 配置统一为 Java 21。
- [x] 整理父子 POM，使版本和依赖由父 POM 集中管理。
- [x] 建立 Spring Boot 启动类并运行第一个健康检查接口。
- [x] 明确模块依赖并画一张简单依赖图。
- [x] 添加统一返回对象、业务异常和全局异常处理。
- [x] 创建 Docker Compose 基础环境。
- [x] 为第一个接口编写测试。

以上内容已全部完成，下一步进入 Part 2：用户注册、登录与权限。

## 12. 决策与问题记录

每次发生重要设计变化时在这里记录，避免项目后期忘记原因。

| 日期 | 决策/问题 | 选择 | 原因 | 后续验证 |
|---|---|---|---|---|
| 2026-07-18 | 初始架构 | Maven 多模块、模块化单体 | 控制复杂度，同时练习模块边界 | 单体稳定后再评估服务拆分 |
| 2026-07-18 | Java 版本 | 统一使用 Java 21 | 当前主流 LTS，生态与部署更稳妥 | 构建前检查各模块配置 |
| 2026-07-18 | Spring Boot 版本 | 从 4.1.0 调整为 3.5.16 | 学习资料和第三方生态更成熟，同时保留现代 Spring 6 技术栈 | 项目完成后可用分支练习升级 Boot 4 |
| 2026-08-05 | 进度口径 | 不按“周”推进，改用 Part 1 ~ Part 8 | 实际开发没有按周执行，周为单位只增加心理压力 | 每个 Part 完成后更新状态并验收 |
| 2026-08-05 | 仓库策略 | 先保持 backend 独立开发，完成后在 D:\LearnHub 建立汇总仓库 | 前后端最终可以放在同一汇总仓库；backend 旧历史保留在本机备份并单独提供参考分支 | 后续加入 frontend，再决定是否拆出独立仓库 |
| 2026-08-05 | Part 1 完成 | 工程骨架、统一响应/异常、示例接口、测试、OpenAPI、Docker Compose 均完成 | 达成 Part 1 验收标准 | 进入 Part 2 用户/登录/权限 |

---

维护规则：每天更新任务勾选；每周末补充一次复盘；遇到值得讲的 Bug，记录现象、排查过程、根因和修复，而不只记录最终答案。
