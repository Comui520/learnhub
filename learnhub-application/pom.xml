<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>com.github.comui520</groupId>
        <artifactId>learnhub</artifactId>
        <version>0.0.1-SNAPSHOT</version>
    </parent>

    <artifactId>learnhub-application</artifactId>
    <name>learnhub-application</name>

    <dependencies>
        <!-- Part 1 的统一响应和业务异常位于 common。 -->
        <dependency>
            <groupId>com.github.comui520</groupId>
            <artifactId>learnhub-common</artifactId>
        </dependency>

        <!-- Session A：把 user 模块组合进来（它带着 Security/MyBatis-Plus/Flyway/迁移文件）。 -->
        <dependency>
            <groupId>com.github.comui520</groupId>
            <artifactId>learnhub-user</artifactId>
        </dependency>

        <dependency>
            <groupId>com.github.comui520</groupId>
            <artifactId>learnhub-knowledge</artifactId>
        </dependency>

        <!-- 提供 Spring MVC、内嵌 Tomcat 和 JSON 序列化。 -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-web</artifactId>
        </dependency>

        <dependency>
            <groupId>com.baomidou</groupId>
            <artifactId>mybatis-plus-jsqlparser</artifactId>
        </dependency>

        <!-- 提供 @Valid、@NotBlank、@Size 等参数校验。 -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-validation</artifactId>
        </dependency>

        <!-- 提供 /actuator/health。 -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-actuator</artifactId>
        </dependency>

        <!-- 生成 OpenAPI JSON 和 Swagger UI。 -->
        <dependency>
            <groupId>org.springdoc</groupId>
            <artifactId>springdoc-openapi-starter-webmvc-ui</artifactId>
        </dependency>

        <!-- 让 IDEA 对 @ConfigurationProperties 提供提示。 -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-configuration-processor</artifactId>
            <optional>true</optional>
        </dependency>

        <!-- 仅用于本地开发自动重启，不传递给下游。 -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-devtools</artifactId>
            <scope>runtime</scope>
            <optional>true</optional>
        </dependency>

        <!-- JUnit 5、Mockito、AssertJ、MockMvc 等测试工具。 -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>com.mysql</groupId>
            <artifactId>mysql-connector-j</artifactId>
            <scope>runtime</scope>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-maven-plugin</artifactId>
            </plugin>
        </plugins>
    </build>
</project>
